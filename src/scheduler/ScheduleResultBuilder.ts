import {IScheduler, SchedRoom, SchedSection, TimeSlot} from "./IScheduler";

export  class ScheduleResultBuilder {

    public buildResult(sections: SchedSection, rooms: SchedRoom, slot: TimeSlot): void {
        return;
    }
}
